# Todo
- [ ] Refactor all commands into functions with index as argument 
```python
def this(index: int) -> None:
    doing stuff...

# For if statements:
if_true = this
if condition:
    if_true(index)

```